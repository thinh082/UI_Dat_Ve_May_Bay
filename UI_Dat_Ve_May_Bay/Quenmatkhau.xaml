<Window x:Class="UI_Dat_Ve_May_Bay.Quenmatkhau"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI_Dat_Ve_May_Bay"
        mc:Ignorable="d"
        Title="Quên Mật Khẩu - Parador" 
        Height="600" 
        Width="950"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="40,12,12,12"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Branding -->
        <Grid Grid.Column="0">
            <!-- Background Image -->
            <Image Source="pack://application:,,,/Images/airplane_ticket.png" 
                   Stretch="UniformToFill"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
        </Grid>

        <!-- Right Panel - Forgot Password Form -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="60,40,60,40" MaxWidth="400">
                <!-- Header -->
                <TextBlock Text="Quên Mật khẩu" 
                          FontFamily="Segoe UI"
                          FontSize="28"
                          FontWeight="Bold"
                          Foreground="#333333"
                          Margin="0,20,0,10"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Đặt lại mật khẩu tài khoản của bạn. Vui lòng nhập Email để nhận mã OTP." 
                          FontFamily="Segoe UI"
                          FontSize="14"
                          Foreground="#666666"
                          TextWrapping="Wrap"
                          TextAlignment="Center"
                          Margin="0,0,0,30"/>

                <!-- Email Input -->
                <Grid Margin="0,0,0,15">
                    <Border Background="White" 
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="8"
                            Height="45"/>
                    <TextBlock Text="✉" 
                              FontSize="16"
                              Foreground="#999999"
                              VerticalAlignment="Center"
                              Margin="15,0,0,0"
                              IsHitTestVisible="False"/>
                    <TextBlock Text="Email" 
                              FontFamily="Segoe UI"
                              FontSize="14"
                              Foreground="#999999"
                              VerticalAlignment="Center"
                              Margin="40,0,0,0"
                              IsHitTestVisible="False"
                              x:Name="txtEmailPlaceholder"/>
                    <TextBox x:Name="txtEmail"
                            FontFamily="Segoe UI"
                            FontSize="14"
                            Padding="40,12,12,12"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="#333333"
                            VerticalContentAlignment="Center"
                            Height="45"/>
                </Grid>

                <!-- Send OTP Button -->
                <Button Content="Gửi mã OTP"
                       Style="{StaticResource ModernButton}"
                       Background="#0066FF"
                       Foreground="White"
                       FontSize="15"
                       Height="45"
                       Margin="0,0,0,20"
                       Click="BtnSendOTP_Click"/>

                <!-- Navigation Links -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe UI" FontSize="14" Margin="0,0,20,0">
                         <Hyperlink TextDecorations="None" 
                                   Foreground="#0066CC"
                                   FontWeight="SemiBold"
                                   Click="BtnLogin_Click">
                             Quay lại đăng nhập
                         </Hyperlink>
                    </TextBlock>
                    <TextBlock FontFamily="Segoe UI" FontSize="14">
                        <Hyperlink TextDecorations="None" 
                                  Foreground="#666666">
                            Cần hỗ trợ?
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
