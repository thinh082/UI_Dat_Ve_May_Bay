<UserControl x:Class="UI_Dat_Ve_May_Bay.Views.BookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:UI_Dat_Ve_May_Bay.ViewModels"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             d:DataContext="{d:DesignInstance Type=viewmodels:BookingViewModel}"
             mc:Ignorable="d"
             Loaded="BookingView_Loaded"
             Unloaded="BookingView_Unloaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style TargetType="ToggleButton" x:Key="SeatToggle">
            <Setter Property="Width" Value="58"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="IsEnabled" Value="{Binding IsAvailable}"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#F59E0B"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#D1D5DB"/>
                    <Setter Property="Foreground" Value="#374151"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="Card">
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: Seat + Flight -->
        <StackPanel Grid.Column="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="← Quay lại" Padding="14,8" Margin="0,0,12,0" Command="{Binding BackCommand}"/>
                <TextBlock FontSize="20" FontWeight="Bold" Text="Chọn ghế + Thanh toán"/>
            </StackPanel>

            <Border Margin="0,12,0,0" Style="{StaticResource Card}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" Text="{Binding SelectedSchedule.HangBayName}"/>
                        <TextBlock Margin="0,4,0,0" Text="{Binding SelectedSchedule.MaSanBayDi}"/>
                        <TextBlock Text="{Binding SelectedSchedule.MaSanBayDen}"/>
                        <TextBlock Margin="0,4,0,0" Text="{Binding SelectedSchedule.GioDi}"/>
                        <TextBlock Text="{Binding SelectedSchedule.GioDen}"/>
                        <TextBlock Margin="0,8,0,0" FontWeight="Bold">
                            <Run Text="Giá: "/><Run Text="{Binding SelectedSchedule.Gia, StringFormat={}{0:n0}}"/><Run Text=" đ / ghế"/>
                        </TextBlock>
                        <TextBlock Margin="0,6,0,0" Foreground="#6B7280" Text="{Binding SelectedSchedule.TienIchText}"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#EEF2FF" CornerRadius="999" Padding="10,4" VerticalAlignment="Top">
                        <TextBlock Foreground="#3730A3" FontWeight="SemiBold" Text="{Binding SelectedSeatCount, StringFormat=Đã chọn: {0} ghế}"/>
                    </Border>
                </Grid>
            </Border>

            <Border Margin="0,12,0,0" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Background="#3B82F6" CornerRadius="10,10,0,0" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Foreground="White" FontWeight="Bold" FontSize="15" Text="Cabin • Sơ đồ ghế"/>
                                <TextBlock Foreground="#DCEBFF" Text="Chia 2 bên như máy bay (trái | lối đi | phải)"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Làm mới ghế" Padding="10,6" Command="{Binding RefreshSeatsCommand}"/>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1" Margin="10">
                        <Border HorizontalAlignment="Left" Width="16" Background="#F3F4F6" CornerRadius="8"/>
                        <Border HorizontalAlignment="Right" Width="16" Background="#F3F4F6" CornerRadius="8"/>
                        <Border VerticalAlignment="Top" Height="18" CornerRadius="0,0,14,14" Margin="48,0,48,0" Background="#F3F4F6"/>

                        <Grid Margin="14,22,14,8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <WrapPanel Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                                    <Ellipse Width="10" Height="10" Fill="#D1D5DB" Margin="0,0,6,0"/>
                                    <TextBlock Text="Đã đặt/không chọn" Foreground="#6B7280"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                                    <Ellipse Width="10" Height="10" Fill="#F59E0B" Margin="0,0,6,0"/>
                                    <TextBlock Text="Bạn giữ" Foreground="#6B7280"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="#3B82F6" Margin="0,0,6,0"/>
                                    <TextBlock Text="Còn trống" Foreground="#6B7280"/>
                                </StackPanel>
                            </WrapPanel>

                            <TabControl Grid.Row="1">
                                <TabItem Header="Phổ thông (loại vé 1)">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding SeatRowsLoai1}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="42"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="34"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding RowLabel}" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        <ItemsControl Grid.Column="1" ItemsSource="{Binding LeftSeats}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <ToggleButton Style="{StaticResource SeatToggle}" Content="{Binding SoGhe}" IsChecked="{Binding IsSelected}" Checked="Seat_Checked" Unchecked="Seat_Unchecked"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <Border Grid.Column="2" Background="#F3F4F6" CornerRadius="8" Margin="3,0"/>
                                                        <ItemsControl Grid.Column="3" ItemsSource="{Binding RightSeats}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <ToggleButton Style="{StaticResource SeatToggle}" Content="{Binding SoGhe}" IsChecked="{Binding IsSelected}" Checked="Seat_Checked" Unchecked="Seat_Unchecked"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </TabItem>

                                <TabItem Header="Thương gia (loại vé 3)">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding SeatRowsLoai3}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="42"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="34"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding RowLabel}" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        <ItemsControl Grid.Column="1" ItemsSource="{Binding LeftSeats}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <ToggleButton Style="{StaticResource SeatToggle}" Content="{Binding SoGhe}" IsChecked="{Binding IsSelected}" Checked="Seat_Checked" Unchecked="Seat_Unchecked"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <Border Grid.Column="2" Background="#F3F4F6" CornerRadius="8" Margin="3,0"/>
                                                        <ItemsControl Grid.Column="3" ItemsSource="{Binding RightSeats}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <ToggleButton Style="{StaticResource SeatToggle}" Content="{Binding SoGhe}" IsChecked="{Binding IsSelected}" Checked="Seat_Checked" Unchecked="Seat_Unchecked"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </TabItem>

                                <TabItem Header="Hạng nhất (loại vé 4)">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding SeatRowsLoai4}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="42"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="34"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding RowLabel}" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        <ItemsControl Grid.Column="1" ItemsSource="{Binding LeftSeats}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <ToggleButton Style="{StaticResource SeatToggle}" Content="{Binding SoGhe}" IsChecked="{Binding IsSelected}" Checked="Seat_Checked" Unchecked="Seat_Unchecked"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <Border Grid.Column="2" Background="#F3F4F6" CornerRadius="8" Margin="3,0"/>
                                                        <ItemsControl Grid.Column="3" ItemsSource="{Binding RightSeats}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <WrapPanel/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <ToggleButton Style="{StaticResource SeatToggle}" Content="{Binding SoGhe}" IsChecked="{Binding IsSelected}" Checked="Seat_Checked" Unchecked="Seat_Unchecked"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </TabItem>
                            </TabControl>
                        </Grid>
                    </Grid>

                    <Border Grid.Row="2" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="10" CornerRadius="0,0,10,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Foreground="#6B7280" Text="Ghế xám: đã đặt/không chọn • Ghế cam: bạn giữ • Ghế xanh: trống"/>
                            <TextBlock Grid.Column="1" FontWeight="Bold" Text="{Binding SelectedSeatCount, StringFormat=Đã chọn: {0} ghế}"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <TextBlock Margin="0,10,0,0" Foreground="#DC2626" TextWrapping="Wrap" Text="{Binding Error}"/>
            <TextBlock Margin="0,4,0,0" Foreground="#065F46" TextWrapping="Wrap" Text="{Binding Info}"/>
        </StackPanel>

        <!-- RIGHT: Summary + Voucher + Payment -->
        <StackPanel Grid.Column="2">
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="Bold" Text="Tóm tắt đặt vé"/>
                    <DockPanel Margin="0,10,0,0">
                        <TextBlock Text="Tạm tính:"/>
                        <TextBlock DockPanel.Dock="Right"><Run Text="{Binding Subtotal, StringFormat={}{0:n0}}"/><Run Text=" đ"/></TextBlock>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock Text="Giảm giá:"/>
                        <TextBlock DockPanel.Dock="Right"><Run Text="{Binding Discount, StringFormat={}{0:n0}}"/><Run Text=" đ"/></TextBlock>
                    </DockPanel>
                    <Separator Margin="0,8"/>
                    <DockPanel>
                        <TextBlock FontWeight="Bold" Text="Tổng:"/>
                        <TextBlock DockPanel.Dock="Right" FontWeight="Bold"><Run Text="{Binding Total, StringFormat={}{0:n0}}"/><Run Text=" đ"/></TextBlock>
                    </DockPanel>
                </StackPanel>
            </Border>

            <Border Margin="0,12,0,0" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock FontWeight="Bold" Text="Voucher"/>
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Height="30" Text="{Binding VoucherCode, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Grid.Column="2" Content="Áp dụng" Padding="12,4" Command="{Binding ApplyVoucherCommand}"/>
                    </Grid>
                    <ComboBox Margin="0,8,0,0" Height="30" ItemsSource="{Binding MyVouchers}" SelectedItem="{Binding SelectedVoucher}" DisplayMemberPath="DisplayText"/>
                    <Button Margin="0,8,0,0" Content="Bỏ voucher" Command="{Binding ClearVoucherCommand}"/>
                </StackPanel>
            </Border>

            <Border Margin="0,12,0,0" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock FontWeight="Bold" Text="Phương thức thanh toán (Direct / VNPAY / PayPal)"/>
                    <ComboBox Margin="0,8,0,0" Height="30" ItemsSource="{Binding PaymentOptions}" SelectedItem="{Binding SelectedPayment}"/>
                    <Button Margin="0,10,0,0" Height="40" Content="Đặt vé" Command="{Binding BookCommand}"/>
                    <Button Margin="0,8,0,0" Height="34" Content="Kiểm tra thanh toán" Command="{Binding CheckPaymentStatusCommand}"/>
                    <TextBlock Margin="0,8,0,0" Foreground="#1F2937" TextWrapping="Wrap" Text="{Binding LatestPaymentStatus}"/>
                    <ListBox Margin="0,8,0,0" Height="110" ItemsSource="{Binding PaymentHistory}" DisplayMemberPath="DisplayLine"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Overlay WebView2 thanh toán trong app -->
        <Grid Grid.ColumnSpan="3" Background="#99000000" Visibility="{Binding IsWaitingPayment, Converter={StaticResource BoolToVis}}">
            <Border Width="1100" Height="680" Background="White" CornerRadius="12" Padding="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Background="#111827" CornerRadius="12,12,0,0" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Foreground="White" FontWeight="Bold" FontSize="15" Text="Thanh toán trong ứng dụng"/>
                                <TextBlock Foreground="#D1D5DB" Text="VNPAY / PayPal chạy trong WebView2 (không mở browser ngoài)"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Kiểm tra" Margin="0,0,8,0" Padding="10,6" Command="{Binding CheckPaymentStatusCommand}"/>
                                <Button Content="Tôi đã thanh toán" Margin="0,0,8,0" Padding="10,6" Command="{Binding ConfirmPaymentCommand}"/>
                                <Button Content="Đóng" Padding="10,6" Click="ClosePaymentOverlay_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <wv2:WebView2 x:Name="PaymentWeb" Grid.Row="1" Margin="8"/>

                    <Border Grid.Row="2" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="10" CornerRadius="0,0,12,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock TextWrapping="Wrap" Foreground="#374151" Text="{Binding LatestPaymentStatus}"/>
                            <Button Grid.Column="1" Content="Mở lại link bằng trình duyệt" Padding="10,6" Command="{Binding OpenPaymentLinkCommand}"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
