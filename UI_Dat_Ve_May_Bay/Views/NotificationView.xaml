<UserControl x:Class="UI_Dat_Ve_May_Bay.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: LIST -->
        <Border Grid.Column="0" Background="White" CornerRadius="16" BorderBrush="#FFE5E7EB" BorderThickness="1" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <DockPanel Grid.Row="0" LastChildFill="True">
                    <TextBlock Text="Thông báo" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Right" Text="{Binding Status}" Foreground="#FF6B7280" VerticalAlignment="Center"/>
                </DockPanel>

                <!-- Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <Button Content="⟳ Tải lại" Width="110" Margin="0,0,10,0" Command="{Binding RefreshCommand}"/>
                    <Button Content="Chi tiết" Width="110" Margin="0,0,10,0" Command="{Binding ViewDetailCommand}"/>
                    <Button Content="🗑 Xóa" Width="110" Command="{Binding DeleteCommand}"/>
                </StackPanel>

                <!-- List -->
                <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
                    <ListBox ItemsSource="{Binding Notifications}"
                             SelectedItem="{Binding Selected}"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="0,0,0,10"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="bd"
                                                    CornerRadius="14"
                                                    BorderThickness="1"
                                                    BorderBrush="#FFE5E7EB"
                                                    Background="White"
                                                    Padding="14">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#FFEFF6FF"/>
                                                    <Setter TargetName="bd" Property="BorderBrush" Value="#FF93C5FD"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="BorderBrush" Value="#FFCBD5E1"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding TieuDe}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Foreground="#FF6B7280" FontSize="12" Margin="0,6,0,0">
                                        <TextBlock.Text>
                                            <Binding Path="NgayTao" StringFormat="{}{0:dd/MM/yyyy HH:mm}"/>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- RIGHT: DETAIL -->
        <Border Grid.Column="2" Background="White" CornerRadius="16" BorderBrush="#FFE5E7EB" BorderThickness="1" Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Chi tiết thông báo" FontSize="18" FontWeight="SemiBold"/>

                <ContentControl Grid.Row="2" Content="{Binding SelectedDetail}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedDetail}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>

                    <ContentControl.ContentTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#FFE5E7EB" BorderThickness="1" CornerRadius="12" Padding="14">
                                <StackPanel>
                                    <TextBlock Text="{Binding TieuDe}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"/>

                                    <TextBlock Foreground="#FF6B7280" FontSize="12" Margin="0,6,0,0">
                                        <TextBlock.Text>
                                            <Binding Path="NgayTao" StringFormat="{}{0:dd/MM/yyyy HH:mm}"/>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>

                <!-- Placeholder when null -->
                <Border Grid.Row="2" BorderBrush="#FFE5E7EB" BorderThickness="1" CornerRadius="12" Padding="14">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedDetail}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <TextBlock Text="Chọn 1 thông báo để xem chi tiết."
                               Foreground="#FF6B7280"/>
                </Border>

                <!-- Body -->
                <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding SelectedDetail.NoiDung}"
                               TextWrapping="Wrap"
                               FontSize="14"
                               LineHeight="20"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
